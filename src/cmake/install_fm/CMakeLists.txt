install(SCRIPT "${CMAKE_SOURCE_DIR}/src/cmake/install_fm/install_and_bundle.cmake")
if(NOT DEFINED COMMIT_VERSION)
  set(COMMIT_VERSION "0")
endif()
configure_package_installer("D-Flow Flexible Mesh Open" "${CMAKE_SOURCE_DIR}/src/cmake/install_fm/ReadMe.txt" "1" "0" "${COMMIT_VERSION}" "STGZ;TGZ")
