Templates for D-Emissions:

The template input files should be used for setting up a new model
schematisation. Note that the include files under EM\config and
WQ\config are simply examples.

The dynamic data that are referenced are very specific to the
application, so they have not been added.
